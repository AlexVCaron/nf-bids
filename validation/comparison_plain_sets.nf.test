nextflow_workflow {

    name "Plugin Comparison: Plain Sets"
    script "./main.nf"
    workflow "main_workflow"

    test("asl001 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/bids-examples/asl001"
                config = "${baseDir}/configs/config_asl.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("asl002 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/bids-examples/asl002"
                config = "${baseDir}/configs/config_asl.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("eeg_cbm - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/bids-examples/eeg_cbm"
                config = "${baseDir}/configs/config_eeg.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }
}
