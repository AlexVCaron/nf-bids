nextflow_workflow {

    name "Plugin Comparison: Sequential Sets"
    script "./main.nf"
    workflow "main_workflow"

    test("qmri_vfa - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/bids-examples/qmri_vfa"
                config = "${baseDir}/validation/configs/config_vfa.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            // Serialize to normalise types, then strip the machine-specific bidsParentDir field.
            // NIfTI files (.nii.gz / .nii) and all other file types are kept as their relative
            // path strings so that snapshot comparison is machine-independent and never triggers
            // gzip decompression on the dummy stub files used in the test datasets.
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("qmri_irt1 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/bids-examples/qmri_irt1"
                config = "${baseDir}/validation/configs/config_irt1.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("qmri_megre - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/bids-examples/qmri_megre"
                config = "${baseDir}/validation/configs/config_megre.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("qmri_mese - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/bids-examples/qmri_mese"
                config = "${baseDir}/validation/configs/config_mese.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("qmri_mp2rage - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/bids-examples/qmri_mp2rage"
                config = "${baseDir}/validation/configs/config_mp2rage.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("qmri_sa2rage - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/bids-examples/qmri_sa2rage"
                config = "${baseDir}/validation/configs/config_sa2rage.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }
}
