nextflow_workflow {

    name "Plugin Comparison: Custom Datasets"
    script "./main.nf"
    workflow "main_workflow"

    test("ds-dwi - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/custom/ds-dwi"
                config = "${baseDir}/validation/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            // Serialize to normalise types, then strip the machine-specific bidsParentDir field.
            // NIfTI files (.nii.gz / .nii) and all other file types are kept as their relative
            // path strings so that snapshot comparison is machine-independent and never triggers
            // gzip decompression on the dummy stub files used in the test datasets.
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("ds-dwi2 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/custom/ds-dwi2"
                config = "${baseDir}/validation/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("ds-dwi3 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/custom/ds-dwi3"
                config = "${baseDir}/validation/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("ds-dwi4 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/custom/ds-dwi4"
                config = "${baseDir}/validation/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("ds-mrs_fmrs - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/custom/ds-mrs_fmrs"
                config = "${baseDir}/validation/configs/config_mrs.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }

    test("ds-mtsat - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/validation/data/custom/ds-mtsat"
                config = "${baseDir}/validation/configs/config_mtsat.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel.collect()
            def parsed = new groovy.json.JsonSlurper().parseText(groovy.json.JsonOutput.toJson(results))
            def snapshot_data = parsed.collect { item ->
                item.collect { e ->
                    (e instanceof Map && e.containsKey('bidsParentDir'))
                        ? e.findAll { k, v -> k != 'bidsParentDir' }
                        : e
                }
            }
            assert snapshot(snapshot_data).match()
        }
    }
}
