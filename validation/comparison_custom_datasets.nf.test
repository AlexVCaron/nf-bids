nextflow_workflow {

    name "Plugin Comparison: Custom Datasets"
    script "./main.nf"
    workflow "main_workflow"

    test("ds-dwi - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/custom/ds-dwi"
                config = "${baseDir}/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("ds-dwi2 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/custom/ds-dwi2"
                config = "${baseDir}/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("ds-dwi3 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/custom/ds-dwi3"
                config = "${baseDir}/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("ds-dwi4 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/custom/ds-dwi4"
                config = "${baseDir}/configs/config_dwi.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("ds-mrs_fmrs - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/custom/ds-mrs_fmrs"
                config = "${baseDir}/configs/config_mrs.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }

    test("ds-mtsat - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/data/custom/ds-mtsat"
                config = "${baseDir}/configs/config_mtsat.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def json = groovy.json.JsonOutput.toJson(results)
            assert snapshot(new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(json))).match()
        }
    }
}
