nextflow_workflow {

    name "Plugin Comparison: Sequential Sets"
    script "./main.nf"
    workflow "main_workflow"
    test("qmri_vfa - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/test/data/bids-examples/qmri_vfa"
                libbids_sh = "${baseDir}/libBIDS.sh/libBIDS.sh"
                config = "${baseDir}/test/configs/config_vfa.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def canonicalize
            canonicalize = { value ->
                if (value instanceof Map) {
                    def cleaned = value.findAll { k, _ -> k != 'bidsParentDir' }.collectEntries { k, v ->
                        [(k): canonicalize(v)]
                    }
                    return new java.util.TreeMap(cleaned)
                }
                if (value instanceof List) {
                    def normalizedList = value.collect { item -> canonicalize(item) }
                    return normalizedList.sort { a, b ->
                        groovy.json.JsonOutput.toJson(a) <=> groovy.json.JsonOutput.toJson(b)
                    }
                }
                return value
            }
            def json = groovy.json.JsonOutput.toJson(canonicalize(results))
            def root = params.bids_dir.toString().replace("\\", "/")
            def normalizedJson = json.replace(root + "/", "").replace(root, ".")
            def yaml = new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(normalizedJson))
            def snapshotText = yaml.toString().replaceAll(/,\s*bidsParentDir:[^,\]]+/, "")
            try {
                assert snapshot(snapshotText).match()
            } catch (Throwable e) {
                println "[snapshot-debug] content-length=${snapshotText.length()}"
                println "[snapshot-debug] content-head:\n${snapshotText.take(2500)}"
                println "[snapshot-debug] content-tail:\n${snapshotText.takeRight(2500)}"
                throw e
            }
        }
    }

    test("qmri_irt1 - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/test/data/bids-examples/qmri_irt1"
                libbids_sh = "${baseDir}/libBIDS.sh/libBIDS.sh"
                config = "${baseDir}/test/configs/config_irt1.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def canonicalize
            canonicalize = { value ->
                if (value instanceof Map) {
                    def cleaned = value.findAll { k, _ -> k != 'bidsParentDir' }.collectEntries { k, v ->
                        [(k): canonicalize(v)]
                    }
                    return new java.util.TreeMap(cleaned)
                }
                if (value instanceof List) {
                    def normalizedList = value.collect { item -> canonicalize(item) }
                    return normalizedList.sort { a, b ->
                        groovy.json.JsonOutput.toJson(a) <=> groovy.json.JsonOutput.toJson(b)
                    }
                }
                return value
            }
            def json = groovy.json.JsonOutput.toJson(canonicalize(results))
            def root = params.bids_dir.toString().replace("\\", "/")
            def normalizedJson = json.replace(root + "/", "").replace(root, ".")
            def yaml = new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(normalizedJson))
            def snapshotText = yaml.toString().replaceAll(/,\s*bidsParentDir:[^,\]]+/, "")
            try {
                assert snapshot(snapshotText).match()
            } catch (Throwable e) {
                println "[snapshot-debug] content-length=${snapshotText.length()}"
                println "[snapshot-debug] content-head:\n${snapshotText.take(2500)}"
                println "[snapshot-debug] content-tail:\n${snapshotText.takeRight(2500)}"
                throw e
            }
        }
    }

    test("qmri_megre - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/test/data/bids-examples/qmri_megre"
                libbids_sh = "${baseDir}/libBIDS.sh/libBIDS.sh"
                config = "${baseDir}/test/configs/config_megre.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def canonicalize
            canonicalize = { value ->
                if (value instanceof Map) {
                    def cleaned = value.findAll { k, _ -> k != 'bidsParentDir' }.collectEntries { k, v ->
                        [(k): canonicalize(v)]
                    }
                    return new java.util.TreeMap(cleaned)
                }
                if (value instanceof List) {
                    def normalizedList = value.collect { item -> canonicalize(item) }
                    return normalizedList.sort { a, b ->
                        groovy.json.JsonOutput.toJson(a) <=> groovy.json.JsonOutput.toJson(b)
                    }
                }
                return value
            }
            def json = groovy.json.JsonOutput.toJson(canonicalize(results))
            def root = params.bids_dir.toString().replace("\\", "/")
            def normalizedJson = json.replace(root + "/", "").replace(root, ".")
            def yaml = new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(normalizedJson))
            def snapshotText = yaml.toString().replaceAll(/,\s*bidsParentDir:[^,\]]+/, "")
            try {
                assert snapshot(snapshotText).match()
            } catch (Throwable e) {
                println "[snapshot-debug] content-length=${snapshotText.length()}"
                println "[snapshot-debug] content-head:\n${snapshotText.take(2500)}"
                println "[snapshot-debug] content-tail:\n${snapshotText.takeRight(2500)}"
                throw e
            }
        }
    }

    test("qmri_mese - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/test/data/bids-examples/qmri_mese"
                libbids_sh = "${baseDir}/libBIDS.sh/libBIDS.sh"
                config = "${baseDir}/test/configs/config_mese.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def canonicalize
            canonicalize = { value ->
                if (value instanceof Map) {
                    def cleaned = value.findAll { k, _ -> k != 'bidsParentDir' }.collectEntries { k, v ->
                        [(k): canonicalize(v)]
                    }
                    return new java.util.TreeMap(cleaned)
                }
                if (value instanceof List) {
                    def normalizedList = value.collect { item -> canonicalize(item) }
                    return normalizedList.sort { a, b ->
                        groovy.json.JsonOutput.toJson(a) <=> groovy.json.JsonOutput.toJson(b)
                    }
                }
                return value
            }
            def json = groovy.json.JsonOutput.toJson(canonicalize(results))
            def root = params.bids_dir.toString().replace("\\", "/")
            def normalizedJson = json.replace(root + "/", "").replace(root, ".")
            def yaml = new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(normalizedJson))
            def snapshotText = yaml.toString().replaceAll(/,\s*bidsParentDir:[^,\]]+/, "")
            try {
                assert snapshot(snapshotText).match()
            } catch (Throwable e) {
                println "[snapshot-debug] content-length=${snapshotText.length()}"
                println "[snapshot-debug] content-head:\n${snapshotText.take(2500)}"
                println "[snapshot-debug] content-tail:\n${snapshotText.takeRight(2500)}"
                throw e
            }
        }
    }

    test("qmri_mp2rage - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/test/data/bids-examples/qmri_mp2rage"
                libbids_sh = "${baseDir}/libBIDS.sh/libBIDS.sh"
                config = "${baseDir}/test/configs/config_mp2rage.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def canonicalize
            canonicalize = { value ->
                if (value instanceof Map) {
                    def cleaned = value.findAll { k, _ -> k != 'bidsParentDir' }.collectEntries { k, v ->
                        [(k): canonicalize(v)]
                    }
                    return new java.util.TreeMap(cleaned)
                }
                if (value instanceof List) {
                    def normalizedList = value.collect { item -> canonicalize(item) }
                    return normalizedList.sort { a, b ->
                        groovy.json.JsonOutput.toJson(a) <=> groovy.json.JsonOutput.toJson(b)
                    }
                }
                return value
            }
            def json = groovy.json.JsonOutput.toJson(canonicalize(results))
            def root = params.bids_dir.toString().replace("\\", "/")
            def normalizedJson = json.replace(root + "/", "").replace(root, ".")
            def yaml = new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(normalizedJson))
            def snapshotText = yaml.toString().replaceAll(/,\s*bidsParentDir:[^,\]]+/, "")
            try {
                assert snapshot(snapshotText).match()
            } catch (Throwable e) {
                println "[snapshot-debug] content-length=${snapshotText.length()}"
                println "[snapshot-debug] content-head:\n${snapshotText.take(2500)}"
                println "[snapshot-debug] content-tail:\n${snapshotText.takeRight(2500)}"
                throw e
            }
        }
    }

    test("qmri_sa2rage - Plugin output") {
        when {
            params {
                bids_dir = "${baseDir}/test/data/bids-examples/qmri_sa2rage"
                libbids_sh = "${baseDir}/libBIDS.sh/libBIDS.sh"
                config = "${baseDir}/test/configs/config_sa2rage.yaml"
            }
        }
        then {
            assert workflow.success
            assert workflow.out.bids_channel
            def results = workflow.out.bids_channel
            collect()
            def canonicalize
            canonicalize = { value ->
                if (value instanceof Map) {
                    def cleaned = value.findAll { k, _ -> k != 'bidsParentDir' }.collectEntries { k, v ->
                        [(k): canonicalize(v)]
                    }
                    return new java.util.TreeMap(cleaned)
                }
                if (value instanceof List) {
                    def normalizedList = value.collect { item -> canonicalize(item) }
                    return normalizedList.sort { a, b ->
                        groovy.json.JsonOutput.toJson(a) <=> groovy.json.JsonOutput.toJson(b)
                    }
                }
                return value
            }
            def json = groovy.json.JsonOutput.toJson(canonicalize(results))
            def root = params.bids_dir.toString().replace("\\", "/")
            def normalizedJson = json.replace(root + "/", "").replace(root, ".")
            def yaml = new groovy.yaml.YamlBuilder()(new groovy.json.JsonSlurper().parseText(normalizedJson))
            def snapshotText = yaml.toString().replaceAll(/,\s*bidsParentDir:[^,\]]+/, "")
            try {
                assert snapshot(snapshotText).match()
            } catch (Throwable e) {
                println "[snapshot-debug] content-length=${snapshotText.length()}"
                println "[snapshot-debug] content-head:\n${snapshotText.take(2500)}"
                println "[snapshot-debug] content-tail:\n${snapshotText.takeRight(2500)}"
                throw e
            }
        }
    }
}